# Template for production builds. Copy to ".env.production" in this directory before
# `npm run build`, or inject VITE_API_BASE_URL via the CI/CD environment.
# The deployed SPA origin must be listed in the backend CORS_ALLOWED_ORIGINS allowlist.

VITE_API_BASE_URL=https://api.example.com
